const express = require('express');
const router = express.Router();

// Stub for submissions routes
router.get('/', (req, res) => res.json({ message: 'submissions route' }));

module.exports = router;
